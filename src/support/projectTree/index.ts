// Re-export types for convenience
export type { FileInfo, FileNode, ProjectTreeOptions } from '../../data/types';

// Export main class
export { ProjectTree } from './ProjectTree';

// Export renderer utilities
export { renderTree } from './treeRenderer';
